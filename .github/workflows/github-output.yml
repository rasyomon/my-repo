name: GITHUB_OUTPUT
on: push
jobs:
  github_output:
    runs-on: ubuntu-latest
    steps:
      - id: source
        run: echo "result=Hello" >> "{GITHUB_OUTPUT}"
      - env:
          RESULT: ${{ steps.source.output.result }}
        run: echo "${RESULT}"
